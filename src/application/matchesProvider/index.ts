import MatchesProvider, { MatchesContext } from './Provider';

import * as matchActions from './actions';
import * as authTypes from './types';

export { MatchesContext, matchActions, authTypes };

export default MatchesProvider;
