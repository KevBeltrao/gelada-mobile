import AuthenticationProvider, { AuthenticationContext } from './Provider';

import * as actions from './actions';
import * as authTypes from './types';

export { AuthenticationContext, actions, authTypes };

export default AuthenticationProvider;
