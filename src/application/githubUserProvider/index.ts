import GithubUserProvider, { GithubUserContext } from './Provider';

import * as actions from './actions';
import * as githubUserTypes from './types';

export { GithubUserContext, actions, githubUserTypes };

export default GithubUserProvider;
